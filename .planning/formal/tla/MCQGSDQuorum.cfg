\* .planning/formal/tla/MCQGSDQuorum.cfg
\* Was GENERATED by bin/xstate-to-tla.cjs from src/machines/qgsd-workflow.machine.ts,
\* which no longer exists (removed in the qgsd→nf rename). QGSDQuorum.tla is now a
\* byte-identical orphaned duplicate of NFQuorum.tla; this cfg is hand-maintained until
\* the duplicate is deduped. The safety invariants below mirror MCsafety and are each
\* TLC-verified to hold under these constants (Agents={a1,a2,a3}, MaxDeliberation=9).

SPECIFICATION Spec
CONSTANTS
  Agents = {a1, a2, a3}
  MaxDeliberation = 9
  MaxSize = 3
SYMMETRY AgentSymmetry
INVARIANT TypeOK
INVARIANT UnanimityMet
INVARIANT QuorumCeilingMet
INVARIANT DeliberationBounded
CHECK_DEADLOCK FALSE
